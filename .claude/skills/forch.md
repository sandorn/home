---
title: forch
description: 使用 ruff 格式化并修复所有 Python 代码
---

Run `ruff format .` and `ruff check --fix --unsafe-fixes .` to format and fix all Python code in the project.

```powershell
ruff format .
ruff check --fix --unsafe-fixes .
basedpyright .
```
