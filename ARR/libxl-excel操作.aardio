import console; 
import libxl;

var excel = libxl();

var sheet = excel.addSheet("11");

sheet.Range("A1B6").merge();

console.dump(
	sheet.Cells("A3").merge
)

console.dump("mergeCount",sheet.mergeCount)

console.dump( 
	sheet.getMerge( sheet.mergeCount ) 
)

excel.Close()

console.pause(true);
